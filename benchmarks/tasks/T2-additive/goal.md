This repository has a math module with `add` and `sub`, and a test suite that also
expects a `mul(a, b)` function which does not exist yet — so the tests currently fail.
Add `mul` so that the whole test suite passes. Do not change anything else.
