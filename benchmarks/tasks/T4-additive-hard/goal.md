This repository is a tiny RPN (reverse-Polish) calculator with `add`, `sub`, `mul`,
and `div` operators, and a test suite that also expects a `pow` operator (raise to a
power) which does not exist yet — so those tests currently fail. Add the `pow` operator
so that the whole test suite passes. Do not change anything else.
