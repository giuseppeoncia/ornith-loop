This repository's greeting function currently returns an English greeting, but the test
suite expects a Spanish one, so the tests fail. Change the greeting so the tests pass.
Leave the rest of the code exactly as it is.
