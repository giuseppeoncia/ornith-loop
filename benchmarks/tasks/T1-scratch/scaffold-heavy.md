Do exactly these steps in order:
1. Use the Write tool to create `fizzbuzz.mjs`.
2. In it, write a `for` loop with a counter `i` from 1 to 15 inclusive.
3. Inside the loop, compute `i % 3` and `i % 5`.
4. If both are 0, `console.log("FizzBuzz")`; else if `i % 3` is 0, `console.log("Fizz")`;
   else if `i % 5` is 0, `console.log("Buzz")`; else `console.log(i)`.
5. Save the file.
6. Run `node fizzbuzz.mjs` with the Bash tool and confirm the output matches.
