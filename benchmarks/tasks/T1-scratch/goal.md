Create a file `fizzbuzz.mjs` that, when run with `node fizzbuzz.mjs`, prints the
numbers 1 through 15 each on its own line — except multiples of 3 print `Fizz`,
multiples of 5 print `Buzz`, and multiples of both 3 and 5 print `FizzBuzz`.
