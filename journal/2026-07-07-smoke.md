# smoke — 2026-07-07

- **Model:** ornith-1.0-9b-64k
- **Task (goal given to ornith):** Create a file `hello.txt` in the current directory containing exactly the single line `hi from ornith`.
- **Grounding supplied:** none beyond the goal — inaugural smoke test of the `orn` toolchain against real ornith on Ollama, deliberately a minimal write-from-scratch task (the "clean" case per the hard-won notes).
- **Run record:** runs/2026-07-07T16-55-52-459Z_smoke.json (ephemeral; key facts embedded below)

## What the self-scaffold did
- Exit: completed, stopReason `stop`, 12.7s (cold run, includes model load)
- Tool sequence (2): write `hello.txt` (ok) → read `hello.txt` (ok)
- Thinking blocks: 3
- Flags: tool-call-as-text? no · stopped-before-tool-call? no · claimed-done? yes · claimed-done-no-change? no
- Self-scaffolding observed: nobody told it to verify — it chose to **write the file, then read it back** to check its own work. That read-after-write is exactly the kind of self-built error-recovery step the project exists to study.

## External verification (Claude)
- How verified: inspected the actual file in `--workdir` (not ornith's self-report). `cat` + `xxd` on `hello.txt`; `git status --porcelain` on the workdir; cross-read the run-record `toolSequence` args.
- Verdict: **pass**. `hello.txt` exists (15 bytes), content is exactly `hi from ornith\n` (hexdump: `…6f726e6974 68 0a`, one trailing LF, no extra whitespace). `git status` shows `?? hello.txt` (a real new file), matching `workdirChange.changed = true` and the `claimed-done-no-change = false` flag. The `claimed-done` heuristic was *true* this time — confirmed genuine by external check.

## Corrective rounds
- None needed — succeeded on the first round.

## Notes / lessons (grounding for next time)
- Full stack works end-to-end against real ornith: `orn` → `pi --thinking off --mode json` (provider `ollama`) → JSONL parse → run record + summary + flags, all accurate against ground truth.
- This was a write-from-scratch task, ornith's known-clean case. It does NOT exercise the interesting failure modes (in-place edit corruption, `<tool_call>`-as-text under thinking-on, stalling before the last step). Next: an **additive/edit task on a real repo** to see where self-scaffolding breaks and what grounding rescues it.
- `--workdir` git snapshot + `claimed-done-no-change` heuristic pulls its weight: it turns "the model says done" into an externally-checkable signal. Here it correctly agreed with the truth; its value shows when the model confabulates.
- Cold-run latency ~13s includes the 9.5GB model load; warm runs will be faster. Budget timeouts accordingly for larger tasks.
